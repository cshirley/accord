---
name: dev
description: Legacy alias for the ACCORD harness skill. Prefer `/skill:accord` or the `/dev` command; this shim exists only for old direct `/skill:dev` calls.
model: sonnet
---

# Legacy Alias For ACCORD

This skill is deprecated. Treat any invocation of `/skill:dev ...` as an invocation of `/skill:accord ...` with the same arguments.

Before doing harness work, read `../accord/SKILL.md` and follow it exactly. The canonical skill name, prompt, and workflow source of truth are now `accord`.
