{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "orchestration-judgment-packet.json",
  "title": "ACCORD orchestration judgment packet",
  "description": "Bounded LLM output for harness brief supplements. Must never include routing fields (no agent / subagent names). Validated in core before merging into outbound subagent task text.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "brief_appendix"],
  "properties": {
    "schema_version": {
      "const": "1.0",
      "description": "Packet format version."
    },
    "brief_appendix": {
      "type": "string",
      "minLength": 0,
      "maxLength": 8000,
      "description": "Plain-text or markdown fragment appended to the harness task under a Judgment supplement heading."
    },
    "focus_points": {
      "type": "array",
      "maxItems": 24,
      "items": {
        "type": "string",
        "maxLength": 500
      },
      "description": "Optional short bullets the harness may echo; never used to select agents."
    }
  }
}
