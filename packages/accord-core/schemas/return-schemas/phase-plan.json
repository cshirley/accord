{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "return-schemas/phase-plan.json",
  "title": "phase-plan return packet",
  "type": "object",
  "required": ["status", "usage"],
  "additionalProperties": false,
  "properties": {
    "status": { "enum": ["done", "needs_input", "stuck"] },
    "draft": { "type": "object" },
    "questions": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "stage", "text"],
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "stage": { "enum": ["delegation_check", "engineer_guidance", "task_decomposition"] },
          "text": { "type": "string", "minLength": 1 }
        }
      }
    },
    "plan_path": { "type": "string", "minLength": 1 },
    "review_findings_remaining": { "type": "integer", "minimum": 0 },
    "question": { "type": "string", "minLength": 1 },
    "context": { "type": "string" },
    "tried": { "type": "string" },
    "usage": {
      "type": "object",
      "required": ["prompt_tokens", "completion_tokens"],
      "additionalProperties": false,
      "properties": {
        "prompt_tokens": { "type": "integer", "minimum": 0 },
        "completion_tokens": { "type": "integer", "minimum": 0 }
      }
    }
  },
  "allOf": [
    {
      "if": {
        "properties": { "status": { "const": "needs_input" } },
        "required": ["status"]
      },
      "then": { "required": ["draft", "questions"] }
    },
    {
      "if": {
        "properties": { "status": { "const": "done" } },
        "required": ["status"]
      },
      "then": { "required": ["plan_path"] }
    },
    {
      "if": {
        "properties": { "status": { "const": "stuck" } },
        "required": ["status"]
      },
      "then": { "required": ["question", "context"] }
    }
  ]
}
