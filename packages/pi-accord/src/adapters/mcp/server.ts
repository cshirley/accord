/**
 * @deprecated Use `bun run packages/accord-mcp/src/server.ts` or `bun run mcp`.
 * Delegates to `@clive.shirley/accord-mcp` for backward compatibility.
 */
export {};

await import("@clive.shirley/accord-mcp/server");
